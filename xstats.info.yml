name: External Statistics
description: Overrides core statistics module to enable it to use external datastores (Currently supports redis only).
package: Custom
type: module
version: 1.0
core: 8.x

dependencies:
    - drupal:statistics
